{
  "base": "ubuntu-22",
  "install": "sudo apt-get update && sudo apt-get install -y build-essential pkg-config libssl-dev libsqlite3-dev clang llvm mold ripgrep bc && curl -sSf https://sh.rustup.rs | sh -s -- -y && ~/.cargo/bin/rustup default stable && ~/.cargo/bin/rustup component add rustfmt clippy && ~/.cargo/bin/cargo install cargo-nextest cargo-tarpaulin just taplo-cli sqlx-cli --locked || true",
  "env": {
    "OPENAI_API_KEY": "@secret:openai",
    "GITHUB_TOKEN": "@secret:github_token",
    "RUST_BACKTRACE": "1",
    "CARGO_TERM_COLOR": "always",
    "CLI_AGENT_LOG_LEVEL": "debug",
    "CLI_AGENT_MEMORY_PATH": "/tmp/.ourcli-dev"
  },
  "postInstall": [
    "echo '🤖 CLI-Agent development environment ready'",
    "echo '📋 Run: bash scripts/agent-checklist.sh'",
    "echo '🧪 Run: bash scripts/pre-commit.sh'",
    "mkdir -p /tmp/.ourcli-dev/projects/dev",
    "echo '💾 Development memory store initialized'"
  ]
}