# CTL Annotation Remover

## Описание

Мощный скрипт для полного удаления всех CTL (Claude Tensor Language) аннотаций из кодовой базы MAGRAY CLI. Предназначен для подготовки проекта к новой минималистичной архитектурной системе.

## Возможности

### 🔍 Автоматический поиск
- **Рекурсивное сканирование** всех `.rs` файлов в `crates/`
- **Интеллектуальное распознавание** различных типов CTL аннотаций
- **Подробная статистика** найденных аннотаций по типам и файлам

### 🗑️ Безопасное удаление
- **Автоматический backup** всех файлов перед изменениями
- **Параллельная обработка** для максимальной скорости
- **Режим предварительного просмотра** (dry-run) для проверки изменений

### 📊 Подробная отчетность
- **Детализированный отчет** по каждому файлу
- **Статистика по типам** аннотаций
- **JSON экспорт** результатов

### ✅ Валидация результатов
- **Автоматическая проверка компиляции** после удаления
- **Контроль целостности** кодовой базы
- **Откат к backup** при ошибках

## Поддерживаемые типы аннотаций

Скрипт распознает и удаляет следующие типы CTL аннотаций:

```rust
// @component: {"k":"C","id":"name","t":"description","m":{"cur":70,"tgt":95,"u":"%"}}
/// @component: {"k":"C","id":"name"}
// @ctl4: {...}
// CTL v3.0
// CTL v2.0
// CTL vX.Y
// Ⱦ[component:Type] := {...}
// ∇, ⊗, ⊕, ⊙, ⊡ операторы
```

## Установка зависимостей

```bash
pip install rich
```

или используйте файл requirements.txt:

```bash
pip install -r requirements.txt
```

## Использование

### Основные команды

```bash
# Предварительный просмотр (рекомендуется)
py ctl_annotation_remover.py --dry-run

# Реальное удаление аннотаций
py ctl_annotation_remover.py

# Указать путь к проекту
py ctl_annotation_remover.py --project-root /path/to/project

# Сохранить отчет в JSON файл
py ctl_annotation_remover.py --report cleanup_report.json
```

### Примеры использования

#### 1. Безопасная проверка
```bash
# Посмотреть что будет удалено без изменения файлов
py ctl_annotation_remover.py --dry-run --project-root "../../.."
```

#### 2. Полная очистка с отчетом
```bash
# Удалить все аннотации и сохранить подробный отчет
py ctl_annotation_remover.py --project-root "../../.." --report full_cleanup_2025.json
```

#### 3. Проверка результатов
```bash
# После удаления проверить компиляцию
cargo check --workspace
```

## Структура отчета

Скрипт генерирует детальный отчет включающий:

### Общая статистика
- Количество обработанных файлов
- Количество файлов с аннотациями  
- Общее количество удаленных строк
- Время выполнения
- Результат компиляции
- Путь к backup

### Детализация по файлам
- Путь к файлу (относительный)
- Количество удаленных строк
- Размер до/после изменений
- Типы найденных аннотаций

### Статистика по типам
- Количество аннотаций каждого типа
- Распределение по категориям

## Безопасность

### Автоматический backup
Перед любыми изменениями скрипт создает полную копию всех `.rs` файлов в:
```
backups/ctl_cleanup_backup_YYYYMMDD_HHMMSS/crates/
```

### Валидация
После удаления аннотаций автоматически запускается:
```bash
cargo check --workspace
```

При ошибках компиляции скрипт предупреждает о необходимости восстановления из backup.

## Производительность

- **Параллельная обработка**: До 4 файлов одновременно
- **Умный кэш**: Предотвращает повторное сканирование
- **Прогресс-бары**: Отслеживание процесса выполнения
- **Типовая производительность**: 265 файлов за ~0.4 секунды

## Опции командной строки

| Параметр | Описание | По умолчанию |
|----------|----------|-------------|
| `--project-root` | Путь к корню проекта | `../../..` |
| `--dry-run` | Режим предварительного просмотра | Отключен |
| `--report FILE` | Файл для сохранения JSON отчета | Нет |
| `--no-backup` | Не создавать backup (только с --dry-run) | Отключен |

## Примеры отчетов

### Консольный вывод
```
+-------- Итоги очистки --------+
| CTL CLEANUP REPORT            |
|                               |
| Обработано файлов: 119        |
| Файлов с аннотациями: 119     |
| Всего удалено строк: 155      |
| Время выполнения: 0.37 секунд |
| Компиляция: OK Успешно        |
| Backup: /path/to/backup       |
+-------------------------------+
```

### JSON отчет
```json
{
  "total_files_processed": 119,
  "total_annotations_removed": 155,
  "files_with_annotations": 119,
  "backup_location": "/path/to/backup",
  "compilation_success": true,
  "duration_seconds": 0.37,
  "detailed_results": [...]
}
```

## Диагностика проблем

### Ошибки кодировки
На Windows могут возникать проблемы с Unicode символами. Скрипт автоматически использует ASCII аналоги.

### Ошибки компиляции
При ошибках компиляции:
1. Проверьте backup в `backups/`
2. Восстановите файлы при необходимости
3. Проверьте корректность путей

### Отсутствие cargo
Убедитесь что `cargo` доступен в PATH:
```bash
cargo --version
```

## Лицензия

MIT License - свободное использование и модификация.

## Автор

Claude Code AI Assistant v1.0.0

---

**⚠️ ВАЖНО**: Всегда выполняйте `--dry-run` перед реальным удалением для проверки изменений!