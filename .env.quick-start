# 🚀 MAGRAY CLI Quick Start Configuration
# 
# Этот файл содержит МИНИМАЛЬНУЮ конфигурацию для быстрого запуска
# Для полной настройки смотри .env.example
#
# 🔧 КАК ИСПОЛЬЗОВАТЬ:
# 1. Скопируйте этот файл в .env: `cp .env.quick-start .env`
# 2. Получите OpenAI API ключ: https://platform.openai.com/api-keys
# 3. Замените "your-api-key-here" на ваш реальный ключ
# 4. Запустите: `make start`

# ============================================================================
# ОБЯЗАТЕЛЬНАЯ НАСТРОЙКА - API КЛЮЧ
# ============================================================================

# OpenAI API Key (ОБЯЗАТЕЛЕН для работы AI)
OPENAI_API_KEY=your-api-key-here

# LLM Provider (оставьте как есть)
LLM_PROVIDER=openai

# OpenAI Model (оставьте как есть)
OPENAI_MODEL=gpt-4o-mini

# ============================================================================
# ОСТАЛЬНЫЕ НАСТРОЙКИ (можно не менять)
# ============================================================================

# Log level для TUI режима (убирает лишние логи)
RUST_LOG=error

# ✅ После настройки:
# - Символы больше не будут дублироваться при вводе в TUI
# - AI будет давать реальные ответы вместо "system not available"
# - TUI чат будет работать как Claude Code